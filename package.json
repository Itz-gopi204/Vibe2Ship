{
  "name": "vibe2ship-civic-copilot",
  "version": "1.0.0",
  "description": "Community Hero - Civic AI Co-Pilot with React & FastAPI",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "pip install -r backend/requirements.txt",
    "install:all": "npm run install:frontend && npm run install:backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python -m uvicorn backend.main:app --reload --port 8000",
    "dev": "npx --yes concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  }
}
